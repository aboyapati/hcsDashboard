function initClass() {document.getElementsByTagName('body')[0].className = "alternate";}
if(window.addEventListener) window.addEventListener('load', initClass, false);
else if(window.attachEvent) window.attachEvent('onload', initClass);